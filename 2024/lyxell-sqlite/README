PREPROCESSING

the input data is converted to csv, e.g. via

  sed 's/ \+/,/g' input

no other preprocessing is performed

RUNNING

install sqlite3, e.g.
$ sudo apt install -y sqlite3

then pass the source code into sqlite, e.g.
$ sqlite3 < 01_01.sql
